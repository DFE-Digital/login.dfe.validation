name: $(BuildDefinitionName)_tag-$(BuildID)_at-$(Date:yyyyMMdd)$(Rev:.r)
resources:
  repositories:
    - repository: devopsTemplates
      type: github
      endpoint: DfE-Digital
      name: DFE-Digital/login.dfe.devops
      ref: feature/package-code-quality

trigger:
  branches:
    include:
      - "*"

pr:
  branches:
    include:
      - main

variables:
  - group: dsi-global

stages:
  - stage: codeChecks
    displayName: Code checks
    jobs:
      - template: pipeline/v2/node-project/checks.yml@devopsTemplates
        parameters:
          nodeVersionSpec: 18.17.0

  - stage: publishPackage
    displayName: Publish Package
    dependsOn:
      - codeChecks
    condition: in(dependencies.codeChecks.result, 'Succeeded', 'Skipped')
    jobs:
      - template: pipeline/v2/node-project/publishPackage.yml@devopsTemplates
        parameters:
          nodeVersionSpec: 18.17.0
